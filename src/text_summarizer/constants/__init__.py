from pathlib import Path

# Define the path to the configuration file
# This file typically contains settings and paths used throughout the project
CONFIG_FILE_PATH = Path("config/config.yaml")

# Define the path to the parameters file
# This file usually contains hyperparameters and other parameters needed for model training or other processes
PARAMS_FILE_PATH = Path("params.yaml")
